// this app uses vite instead of meteor's own build system
// the entry point used by vite is in src/client/startup/main.js (referenced in vite.config.js)
// this here is an empty entry point since meteor 3.0.1 expects one and is not aware of vite (referenced in package.json)
